# Base URL of the DocuMind FastAPI backend.
# Leave empty to use the Vite dev proxy (recommended in development):
#   /api/*   -> http://127.0.0.1:8010/*
#   /files/* -> http://127.0.0.1:8010/files/*
# In production, set this to the deployed API origin, e.g. https://api.example.com
VITE_API_URL=
